export default function Loading() {
  return (
    <div
      style={{
        height: "100vh",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        background: "var(--wave-bg)",
        color: "var(--wave-blue)",
        fontSize: "2rem",
        fontWeight: "bold",
      }}
    >
      Loading Wave...
    </div>
  );
}
